{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Document Processor",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/src/document_processor.py",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  ]
} 