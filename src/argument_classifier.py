"""
argument_classifier.py
Module for classifying sentences as claims using LLMs.
"""

class ArgumentClassifier:
    """Classifies sentences as claims using a language model."""
    def __init__(self, model_config):
        self.model_config = model_config

    def classify(self, sentence):
        """Classifies a sentence and returns claim type or label."""
        pass 